{
  "name": "incident-commander-ts",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Deterministic decision core of the AI Incident Commander (TypeScript port).",
  "scripts": {
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
